/**
 * Copy that must read identically everywhere it appears.
 */

/**
 * Shown on every screen backed by invented history.
 *
 * Today is deliberately excluded: it renders the goal the user actually
 * created, so labelling it sample data would be the opposite lie.
 */
export const SAMPLE_DATA_NOTE =
  'Sample data. Your own history appears once accounts are added.';
