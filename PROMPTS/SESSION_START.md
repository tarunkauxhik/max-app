Read `AGENTS.md`, `info.md`, `PRODUCT.md`, `ROADMAP.md`, `DECISIONS.md`, `DEPENDENCIES.md`, `LEARNINGS.md`, and `QUALITY_GATES.md`.

Then inspect the current git status and package.json without changing anything.

Report only:
1. current milestone and project state;
2. Expo SDK and package manager from files;
3. relevant installed skills/plugins you can detect;
4. blockers or contradictions;
5. the single smallest useful next step.

Do not edit, install, build, delete, commit or push.
