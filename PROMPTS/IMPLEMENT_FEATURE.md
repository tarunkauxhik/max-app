Implement only the approved plan for: <FEATURE>

Constraints:
- use existing components and dependencies first;
- do not add/upgrade packages without separate approval;
- do not run native builds, deployment or destructive cleanup;
- keep the diff narrow;
- preserve strict TypeScript and security boundaries.

After editing:
1. run only approved static/unit checks;
2. list changed files and purpose;
3. show unresolved warnings or assumptions;
4. give exact Android manual tests and iOS tests when supported;
5. stop before commit or push.
